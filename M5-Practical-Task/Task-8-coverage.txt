TASK 8 - COVERAGE CHECK

Ran all tests with coverage in IntelliJ. All 70 tests pass.

Two untested branches I found:

1. AnimalType.isSenior() - not covered (whole enum is 0%).
   Would a test catch a real bug? YES. It has a boundary: for a dog
   (lifespan 15), 15/2 = 7, so age 7 is not senior but age 8 is. A bug
   could easily hide at that edge, so this is worth testing.

2. AnimalCsvParser.parseFile() - the "if (header == null)" branch is
   never run (only tested with a file that has a header).
   Would a test catch a real bug? YES. An empty file is realistic, and
   without this check the code could crash instead of returning an empty
   result. Worth testing.


